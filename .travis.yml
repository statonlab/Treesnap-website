language: php

php:
  - 7.1

before_script:
  - cp .env.travis .env
  - php artisan key:generate
  - composer self-update
  - composer install --no-interaction
  - npm install


before_install:

script:
#set up db
  - php artisan migrate
  - php artisan db:seed
#run tests
  - npm run production
  - phpunit

services:
 - mysql